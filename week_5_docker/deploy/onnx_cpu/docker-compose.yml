version: "3"
services:
    prediction_api:
        build:
            context: ../..
            dockerfile: deploy/onnx_cpu/Dockerfile
        container_name: "inference_container_cpu"
        ports:
            - "8000:8000"
